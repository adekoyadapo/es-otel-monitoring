apiVersion: v1
kind: ServiceAccount
metadata:
  name: edot-main-logs-api
  namespace: lab-main
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: edot-main-logs-api
rules:
  - apiGroups: [""]
    resources: ["pods", "namespaces", "nodes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["replicasets"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: edot-main-logs-api
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: edot-main-logs-api
subjects:
  - kind: ServiceAccount
    name: edot-main-logs-api
    namespace: lab-main
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: edot-main-logs-api-config
  namespace: lab-main
data:
  config.yaml: |
    receivers:
      filelog/elasticsearch:
        include:
          - /var/log/containers/*_lab-main_elasticsearch-*.log
        start_at: beginning
        include_file_path: true

    processors:
      memory_limiter:
        check_interval: 1s
        limit_mib: 256
      attributes/edot_logs:
        actions:
          - key: data_stream.dataset
            action: upsert
            value: elasticsearch.logs
          - key: data_stream.namespace
            action: upsert
            value: main
      k8sattributes:
        auth_type: serviceAccount
        passthrough: false
        filter:
          node_from_env_var: KUBE_NODE_NAME
      resource/main:
        attributes:
          - key: elasticsearch.cluster.name
            action: upsert
            value: main
          - key: service.name
            action: upsert
            value: elasticsearch-main
          - key: service.namespace
            action: upsert
            value: lab-main
          - key: orchestrator.cluster.name
            action: upsert
            value: edot-lab
      batch:
        send_batch_size: 1024
        timeout: 5s

    exporters:
      otlp_grpc/gateway:
        endpoint: edot-gateway.lab-monitoring.svc.cluster.local:4317
        tls:
          insecure: true

    extensions:
      health_check:
        endpoint: 0.0.0.0:13133

    service:
      extensions: [health_check]
      pipelines:
        logs:
          receivers: [filelog/elasticsearch]
          processors: [memory_limiter, attributes/edot_logs, k8sattributes, resource/main, batch]
          exporters: [otlp_grpc/gateway]
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: edot-main-logs-api
  namespace: lab-main
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: edot-main-logs-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: edot-main-logs-api
    spec:
      serviceAccountName: edot-main-logs-api
      containers:
        - name: edot-main-logs-api
          image: docker.elastic.co/elastic-agent/elastic-agent:__ELASTIC_AGENT_VERSION__
          args: ["--config=/etc/edot/config.yaml"]
          env:
            - name: ELASTIC_AGENT_OTEL
              value: "true"
            - name: KUBE_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          ports:
            - containerPort: 13133
              name: health
          readinessProbe:
            httpGet:
              path: /
              port: 13133
          livenessProbe:
            httpGet:
              path: /health/status
              port: 13133
            initialDelaySeconds: 15
            periodSeconds: 30
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          volumeMounts:
            - name: config
              mountPath: /etc/edot
            - name: varlogcontainers
              mountPath: /var/log/containers
              readOnly: true
            - name: varlogpods
              mountPath: /var/log/pods
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: edot-main-logs-api-config
        - name: varlogcontainers
          hostPath:
            path: /var/log/containers
        - name: varlogpods
          hostPath:
            path: /var/log/pods
