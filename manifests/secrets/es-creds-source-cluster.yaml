# Template — replace the placeholder before applying.
# Validation: curl -H "Authorization: ApiKey <encoded>" \
#   https://elasticsearch-main-es-http.lab-main.svc.cluster.local:9200/_cluster/health
apiVersion: v1
kind: Secret
metadata:
  name: es-creds-source-cluster
  namespace: lab-main
stringData:
  api_key: "REPLACE_WITH_SOURCE_CLUSTER_API_KEY_ENCODED"
