# Template — replace the placeholder before applying.
# Validation: curl -H "Authorization: ApiKey <encoded>" \
#   https://elasticsearch-monitoring-es-http.lab-monitoring.svc.cluster.local:9200/_cluster/health
apiVersion: v1
kind: Secret
metadata:
  name: es-creds-monitoring-cluster
  namespace: lab-monitoring
stringData:
  api_key: "REPLACE_WITH_MONITORING_CLUSTER_API_KEY_ENCODED"
